function sales(v){let arr=data.sales.filter(x=>(x.item+' '+x.status+' '+x.notes).toLowerCase().includes(search.toLowerCase())).sort((a,b)=>b.date.localeCompare(a.date));v.innerHTML=`<div class="toolbar"><input placeholder="Rechercher une vente…" value="${esc(search)}" oninput="search=this.value;render()"><button class="btn primary" onclick="openSale()">＋ Vente</button><button class="btn" onclick="csv('sales')">Exporter CSV</button></div><div class="card">${salesTable(arr)}</div>`}
function expenses(v){let arr=data.expenses.filter(x=>(x.label+' '+x.category+' '+x.notes).toLowerCase().includes(search.toLowerCase())).sort((a,b)=>b.date.localeCompare(a.date));v.innerHTML=`<div class="toolbar"><input placeholder="Rechercher une dépense…" value="${esc(search)}" oninput="search=this.value;render()"><button class="btn primary" onclick="openExpense()">＋ Dépense</button><button class="btn" onclick="csv('expenses')">Exporter CSV</button></div><div class="card"><table class="table"><tr><th>Date</th><th>Libellé</th><th>Catégorie</th><th>Montant</th><th>Récurrent</th><th></th></tr>${arr.map(x=>`<tr><td>${fmt(x.date)}</td><td>${esc(x.label)} ${x.receipt?'📎':''}</td><td>${esc(x.category)}</td><td>${euro(x.amount)}</td><td>${x.recurring?'✓':''}</td><td><button class="btn small" onclick="openExpense('${x.id}')">Modifier</button></td></tr>`).join('')||'<tr><td colspan="6"><div class="empty">Aucune dépense.</div></td></tr>'}</table></div>`}
function stock(v){let arr=data.stock.filter(x=>(x.name+' '+x.brand+' '+x.category+' '+x.location+' '+x.size).toLowerCase().includes(search.toLowerCase()));if(data._sf)arr=arr.filter(x=>x.status===data._sf);let dormant=data.stock.filter(x=>x.status!=='Vendu'&&daysSince(x.date)>30);v.innerHTML=`<div class="toolbar"><input placeholder="Article, marque, emplacement…" value="${esc(search)}" oninput="search=this.value;render()"><select onchange="data._sf=this.value;save();render()"><option value="">Tous les statuts</option>${['En vente','Réservé','Vendu'].map(s=>`<option ${data._sf===s?'selected':''}>${s}</option>`).join('')}</select><button class="btn primary" onclick="openStock()">＋ Article</button><button class="btn" onclick="openAIStockImport()">✨ Importer des captures Vinted</button><button class="btn" onclick="openLot()">＋ Lot</button><button class="btn" onclick="csv('stock')">Exporter CSV</button></div><div class="card"><div class="section-title"><h2>Stock intelligent</h2><span class="pill ${dormant.length?'warn':'good'}">${dormant.length} dormant(s) &gt; 30 jours</span></div><div class="tablewrap"><table class="table"><tr><th>Article</th><th>Marque</th><th>Achat</th><th>Cible</th><th>Profit potentiel</th><th>ROI</th><th>Performance</th><th>Emplacement</th><th></th></tr>${arr.map(x=>{let prof=+x.target-+x.buy,roi=+x.buy?prof/+x.buy*100:0,conv=+x.views?+x.favorites/+x.views*100:0;return `<tr><td>${x.photo?`<img class="thumb" src="${x.photo}">`:''}<b>${esc(x.name)}</b><br><span class="small">${esc(x.size)} · ${esc(x.condition)} · ${daysSince(x.date)} j</span></td><td>${esc(x.brand)}</td><td>${euro(x.buy)}</td><td>${euro(x.target)}</td><td>${euro(prof)}</td><td>${roi.toFixed(0)}%</td><td>${x.views||0} vues · ${x.favorites||0} fav. · ${conv.toFixed(1)}%</td><td>${esc(x.location)}</td><td><button class="btn small" onclick="openStock('${x.id}')">Modifier</button> <button class="btn small" onclick="priceReductionSimulator('${x.id}')">📉 Prix</button></td></tr>`}).join('')}</table></div></div>`}
function toggleOrderStatusFilter(status,checked){if(checked){if(!orderStatusFilters.includes(status))orderStatusFilters.push(status)}else orderStatusFilters=orderStatusFilters.filter(x=>x!==status);render()}
function setOrderStatusFilters(mode){if(mode==='active')orderStatusFilters=['Nouvelle vente','À préparer','À emballer','À expédier','Expédiée','En transit'];if(mode==='all')orderStatusFilters=[...ORDER_STATUSES];if(mode==='none')orderStatusFilters=[];render()}
function toggleOrderPaymentFilter(status,checked){if(checked){if(!orderPaymentFilters.includes(status))orderPaymentFilters.push(status)}else orderPaymentFilters=orderPaymentFilters.filter(x=>x!==status);render()}
function setOrderPaymentFilters(mode){if(mode==='all')orderPaymentFilters=['En attente','Argent reçu'];if(mode==='pending')orderPaymentFilters=['En attente'];if(mode==='received')orderPaymentFilters=['Argent reçu'];if(mode==='none')orderPaymentFilters=[];render()}
function orders(v){let arr=[...data.orders].sort((a,b)=>a.deadline.localeCompare(b.deadline));let statusFilters=orderStatusFilters.length?orderStatusFilters:[];let paymentFilters=orderPaymentFilters.length?orderPaymentFilters:[];let displayed=arr.filter(o=>statusFilters.includes(o.status)&&paymentFilters.includes((data.sales.find(s=>s.id===o.saleId)?.paymentStatus)||'En attente'));v.innerHTML=`<div class="toolbar"><button class="btn primary" onclick="openOrder()">＋ Commande</button></div><div class="card" style="margin-bottom:16px"><div class="section-title"><h2>📦 Filtres des commandes</h2><span class="pill purple">${statusFilters.length} statut(s) · ${paymentFilters.length} paiement(s)</span></div><p class="small">Les catégories de paiement se mettent à jour automatiquement selon le champ <b>Paiement</b> de chaque vente.</p><div class="filter-title">Statuts</div><div class="filter-grid">${ORDER_STATUSES.map(s=>`<label><input type="checkbox" ${statusFilters.includes(s)?'checked':''} onchange="toggleOrderStatusFilter('${s}',this.checked)"> ${s}</label>`).join('')}</div><div class="filter-actions"><button class="btn small" onclick="setOrderStatusFilters('active')">📦 Actives : Nouvelle vente → En transit</button><button class="btn small" onclick="setOrderStatusFilters('all')">Tout afficher</button><button class="btn small" onclick="setOrderStatusFilters('none')">Tout désélectionner</button></div><div class="filter-title" style="margin-top:16px">Paiement</div><div class="filter-grid two"><label><input type="checkbox" ${paymentFilters.includes('En attente')?'checked':''} onchange="toggleOrderPaymentFilter('En attente',this.checked)"> ⏳ En attente</label><label><input type="checkbox" ${paymentFilters.includes('Argent reçu')?'checked':''} onchange="toggleOrderPaymentFilter('Argent reçu',this.checked)"> 💰 Argent reçu</label></div><div class="filter-actions"><button class="btn small" onclick="setOrderPaymentFilters('pending')">⏳ En attente</button><button class="btn small" onclick="setOrderPaymentFilters('received')">💰 Argent reçu</button><button class="btn small" onclick="setOrderPaymentFilters('all')">Tous les paiements</button><button class="btn small" onclick="setOrderPaymentFilters('none')">Aucun paiement</button></div></div><div class="list">${displayed.map(o=>{let sale=data.sales.find(s=>s.id===o.saleId),payment=sale?.paymentStatus||'En attente',late=o.deadline<today()&&!['Livrée','Terminée'].includes(o.status);return `<div class="item"><div><b>${esc(o.item)}</b><div class="small">Vente ${fmt(o.date)} · échéance ${fmt(o.deadline)} ${late?'· EN RETARD':''}</div><div style="margin-top:6px"><span class="pill ${payment==='Argent reçu'?'good':'warn'}">${payment==='Argent reçu'?'💰 Argent reçu':'⏳ En attente'}</span></div></div><div class="actions"><select onchange="setOrder('${o.id}',this.value)">${ORDER_STATUSES.map(s=>`<option ${o.status===s?'selected':''}>${s}</option>`).join('')}</select><button class="btn small" onclick="openOrder('${o.id}')">Modifier</button></div></div>`}).join('')||'<div class="empty">Aucune commande pour les filtres sélectionnés.</div>'}</div>`}
function setOrder(id,s){let o=data.orders.find(x=>x.id===id);if(o){o.status=s;let sale=o.saleId?data.sales.find(x=>x.id===o.saleId):null;if(sale)sale.status=s;save();render()}}
